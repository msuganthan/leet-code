package matrix;

/**
 * We are given a matrix with R rows and C columns has cells with integer co-ordinates (r, c) with 0 <= r < R
 * and 0 <= c < C
 */

/**
 * Return the coordinates of all cells in the matrix, sorted by their distance from (r0, c0) from smallest distance to
 * largest distance. Here, the distance between two cells (r1, c1) and (r2, c2) is the Manhattan distance,
 * |r1 - r2| + |c1 - c2|
 */
public class DistanceOrder {
}
